# Journal — 2026-08-07

## Fait

- Retour de GitHub Actions (panne majeure de la veille) : CI de tête relancée
  → VERTE (valide build.bat chemins core/ + main.c/test_semver sous MSVC).
- **VS-034 livré et TERMINÉ** (b9dfa78) : job CI client-macos (suite C portable
  asan+ubsan, swift test, build .app + garde-fou 10 Mo). Premier run vert, les
  3 jobs (qa-go, client-windows, client-macos) verts ensemble. YAML écrit par
  un agent Sonnet (2 stalls sur les vérifs locales — travail récupéré,
  commandes revalidées par l'orchestrateur).
- **ADR-010 intégralement livré en ~24 h** : les deux clients partagent leur
  couche applicative C (core/), gelée par les 13 vecteurs rejoués sur les deux
  OS à chaque push.
- **VM Win11 installée par Thibault** → pivot du pilotage GUI (coûteux en
  tokens, souris relative inutilisable, clavier BEFR corrompu) vers le
  **contrôle par code** : qemu-guest-agent déjà actif → `utmctl exec`/
  `file push`/`file pull` (exec en SYSTEM, pas de stdout → passer par des
  fichiers ; `file pull` échoue sur un fichier ouvert → copier d'abord).
  Bootstrap OpenSSH Server poussé et exécuté dans la VM (clé dédiée
  `~/.ssh/vibesync_vm_ed25519`, IP 192.168.64.2) — en cours (Windows Update).
  « UTM Server » dans les réglages = pour l'app iOS UTM Remote, inutile ici.
  Guest tools SPICE installés (presse-papiers/dossier partagé après reboot).
- **Décision Thibault : .app en release avec signature ad hoc** (pas de compte
  Apple Developer). Chantier livré (agent Opus, revalidé par l'orchestrateur) :
  vérif dure de signature en CI (build-macos.sh tolère l'échec de codesign, la
  CI non), zip `ditto` sur tag v*, release à 3 needs qui attache exe + .app en
  un seul `gh release create` (rejouable via `upload --clobber`), guides mis à
  jour (2 chemins Gatekeeper dont Sequoia « Ouvrir quand même »).

- **SSH actif dans la VM** (`ssh -i ~/.ssh/vibesync_vm_ed25519 OPMVPC@192.168.64.2`) :
  OpenSSH Server installé par script via le guest agent (~30 min de Windows
  Update ; Add-WindowsCapability resté coincé → étapes restantes rejouées par un
  script idempotent). Piège Windows FR : `icacls ... Administrators` échoue →
  SID `*S-1-5-32-544`/`*S-1-5-18`. Noté en mémoire persistante.
- **VM provisionnée par script** (agent Opus via SSH, `scripts/provision-vm.ps1`
  rejouable/idempotent, tout en portable sans droits admin) : MinGit ARM64,
  Go 1.26.5 arm64, llvm-mingw 20260616 ucrt aarch64, VLC 3.0.23 winarm64, PATH
  persistant HKCU. Repo cloné. **`build.bat test` : 1437 vérifications, 0 échec,
  13/13 vecteurs** (DPAPI/Winsock/WinHTTP réels sous émulation x86_64) ;
  `go build/vet/test ./...` verts (e2e 78,8 s) ; release 252 Ko.
- **Vrai bug débusqué par la VM : `build.bat` avalait les crashs Windows.**
  `if errorlevel 1` compare en signé → un code négatif (0xC0000135 DLL
  introuvable = -1073741515) passait pour un succès, dans `:test` ET `:asan`
  (l'asan « OK » ne démarrait même pas). Fix : `if not "%errorlevel%"=="0"` +
  dérivation de LLVMROOT depuis %CC% pour trouver la DLL ASan (le chemin en dur
  `thibs` + mauvais sous-répertoire ne marchait nulle part). Validé dans la VM.
- **ASan = cul-de-sac sur Windows ARM64** (documenté, pas contournable) :
  l'interception x86_64 ne sait pas réécrire les prologues des DLL système sous
  émulation, et llvm-mingw ne livre pas de runtime ASan aarch64-windows. La VM
  valide donc le client Windows fonctionnellement ; la couverture asan/ubsan du
  C portable reste le job client-macos (même core/src/*.c — cohérent ADR-010).
- Piège `.ps1` : PowerShell 5.1 lit les scripts sans BOM en cp1252 (un tiret
  cadratin UTF-8 y devient un guillemet et décale toutes les chaînes) →
  provision-vm.ps1 en ASCII pur.

## À reprendre

Critères réels Windows de VS-029 dans la VM par SSH (repro vlcrc Syncplay,
détection d'action utilisateur réelle, séance 2 clients C) → tag v0.2.1 (qui
publiera aussi la première .app, décision ad hoc de Thibault). Reliquats :
drapeaux VLC côté Go, captures mac, ini_flush, VS-035. Détails : docs/STATUS.md.
