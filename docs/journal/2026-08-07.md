# Journal — 2026-08-07

## Fait

- Retour de GitHub Actions (panne majeure de la veille) : CI de tête relancée
  → VERTE (valide build.bat chemins core/ + main.c/test_semver sous MSVC).
- **VS-034 livré et TERMINÉ** (b9dfa78) : job CI client-macos (suite C portable
  asan+ubsan, swift test, build .app + garde-fou 10 Mo). Premier run vert, les
  3 jobs (qa-go, client-windows, client-macos) verts ensemble. YAML écrit par
  un agent Sonnet (2 stalls sur les vérifs locales — travail récupéré,
  commandes revalidées par l'orchestrateur).
- **ADR-010 intégralement livré en ~24 h** : les deux clients partagent leur
  couche applicative C (core/), gelée par les 13 vecteurs rejoués sur les deux
  OS à chaque push.
- **VM Win11 installée par Thibault** → pivot du pilotage GUI (coûteux en
  tokens, souris relative inutilisable, clavier BEFR corrompu) vers le
  **contrôle par code** : qemu-guest-agent déjà actif → `utmctl exec`/
  `file push`/`file pull` (exec en SYSTEM, pas de stdout → passer par des
  fichiers ; `file pull` échoue sur un fichier ouvert → copier d'abord).
  Bootstrap OpenSSH Server poussé et exécuté dans la VM (clé dédiée
  `~/.ssh/vibesync_vm_ed25519`, IP 192.168.64.2) — en cours (Windows Update).
  « UTM Server » dans les réglages = pour l'app iOS UTM Remote, inutile ici.
  Guest tools SPICE installés (presse-papiers/dossier partagé après reboot).
- **Décision Thibault : .app en release avec signature ad hoc** (pas de compte
  Apple Developer). Chantier livré (agent Opus, revalidé par l'orchestrateur) :
  vérif dure de signature en CI (build-macos.sh tolère l'échec de codesign, la
  CI non), zip `ditto` sur tag v*, release à 3 needs qui attache exe + .app en
  un seul `gh release create` (rejouable via `upload --clobber`), guides mis à
  jour (2 chemins Gatekeeper dont Sequoia « Ouvrir quand même »).

## À reprendre

VM Win11 : fin du bootstrap SSH → script d'installation toolchain (Go,
llvm-mingw, git, VLC) → build.bat test + asan dans la VM → critères réels
VS-029 → tag v0.2.1 (qui publiera aussi la première .app). Reliquats :
drapeaux VLC côté Go, captures mac, ini_flush, VS-035. Détails : docs/STATUS.md.
