# Journal — 2026-08-05

## Fait

- Kickoff du projet vibesync (session Claude Code).
- Recherche déléguée à 2 agents Sonnet : architecture Syncplay + alternatives/algo de
  sync → rapports dans `docs/research/`.
- Décisions actées avec Thibault : full custom (ADR-001), Go + WebSocket + Coolify
  (ADR-002), VLC via interface HTTP (ADR-003), GUI = web UI locale (ADR-004).
- Bureaucratie mise en place, spec protocole v1 rédigée (`docs/protocol.md`).
- Go installé sur la machine via winget (n'était pas présent) ; git ✅ Docker ✅ VLC ✅.

## Décisions

- Voir ADR-001 à ADR-004. Exigences ajoutées par Thibault en cours de route :
  client graphique soigné Windows + macOS arm64, beaucoup de tests, analyse statique.

## Problèmes → solutions

- Go absent de la machine → installé via `winget install GoLang.Go` (PATH : nouveau
  shell requis ou chemin complet `C:\Program Files\Go\bin\go.exe`).
- Le champ `time` de status.json VLC n'est qu'à la seconde → lire `position × length`
  et corriger finement par rate nudge (spec §sync).

## À reprendre

Voir `docs/STATUS.md`.
