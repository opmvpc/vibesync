# Journal — 2026-08-05

## Fait

- Kickoff du projet vibesync (session Claude Code).
- Recherche déléguée à 2 agents Sonnet : architecture Syncplay + alternatives/algo de
  sync → rapports dans `docs/research/`.
- Décisions actées avec Thibault : full custom (ADR-001), Go + WebSocket + Coolify
  (ADR-002), VLC via interface HTTP (ADR-003), GUI = web UI locale (ADR-004).
- Bureaucratie mise en place, spec protocole v1 rédigée (`docs/protocol.md`).
- Go installé sur la machine via winget (n'était pas présent) ; git ✅ Docker ✅ VLC ✅.

## Décisions

- Voir ADR-001 à ADR-004. Exigences ajoutées par Thibault en cours de route :
  client graphique soigné Windows + macOS arm64, beaucoup de tests, analyse statique.

## Problèmes → solutions

- Go absent de la machine → installé via `winget install GoLang.Go` (PATH : nouveau
  shell requis ou chemin complet `C:\Program Files\Go\bin\go.exe`).
- Le champ `time` de status.json VLC n'est qu'à la seconde → lire `position × length`
  et corriger finement par rate nudge (spec §sync).

## Fait (suite de journée — développement)

- VS-002 protocole (orchestrateur), puis en parallèle par agents Opus : VS-003 serveur
  (review terra), VS-004/005 client de référence (review sol high, 16 findings),
  internal/ws handmade (review sol high sécurité, 11 findings) et migration → **zéro
  dépendance dans go.mod**. Image Docker serveur validée (18 Mo, healthz OK).
- VS-006 : e2e 7 scénarios verts ; bug réel trouvé (rejoin après coupure silencieuse
  → name_taken fatal) corrigé par un jeton de session dans le protocole (spec + code
  + 11 tests + e2e 0,53 s). Harnais du test réel double-VLC écrit, exécution différée
  (VLC occupé par Thibault). Cœur C/Win32 (VS-014) en cours par agent Opus.

## Problèmes → solutions (suite)

- Paquets winget WinLibs ET LLVM-MinGW cassés à l'extraction → réextraction manuelle
  du zip du cache winget (`tar -xf`) vers `C:\Users\thibs\tools\llvm-mingw` ; utiliser
  le wrapper `x86_64-w64-mingw32-clang.exe`, pas `clang.exe` nu.
- `git add -A` pendant qu'un agent écrit → commit pollué, deux fois → règle : staging
  par chemins explicites tant qu'un agent tourne (aussi en mémoire persistante).
- Pipeline PowerShell Get/Set-Content sur fichiers UTF-8 → mojibake, deux fois →
  toujours Read/Edit/Write (aussi en mémoire persistante).
- staticcheck U1000 trompeur quand la compilation des tests échoue (méthodes « mortes »
  en réalité utilisées par les tests) → corriger d'abord les erreurs de compilation.

## À reprendre

Voir `docs/STATUS.md`.
