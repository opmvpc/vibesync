# Journal — 2026-08-06

## Fait

- Review sol high du cœur C : 2 bloquants concurrence WinHTTP + 9 autres → fixes en cours.
- Client Swift complet livré (ui/macos, ~3 600 lignes, moteur + vecteurs + UI + build
  script + docs/build-macos.md) — à compiler sur le Mac.
- Test réel double-VLC dans **Windows Sandbox** (idée de Thibault, harnais
  `scripts/run-real-sandbox.ps1`) : 1er run ÉCHEC → découverte de la course autoplay
  VLC et de la lenteur du nudge au départ ; 2 règles gelées dans la spec ; fixes Go ;
  **2e run PASS 7/7 (drift 0,25 s)**. VS-006 terminé.
- Coupure de quota Claude vers 2h20 → les 3 agents relancés de leur transcript au
  retour. Thibault a créé le remote `github.com/opmvpc/vibesync` et committé l'état
  intermédiaire (bf72f5f) ; désormais **push après chaque commit**.

## Décisions

- Windows Sandbox = outil standard de validation réelle (jetable, VLC du host mappé
  lecture seule, réseau coupé, auto-extinction).

## Problèmes → solutions

- `.ps1` avec here-strings écrit en LF → erreur de parsing PowerShell 5.1 (« terminateur
  manquant ») → convertir en CRLF (noté en mémoire persistante).
- WindowsSandbox.exe ignore `-WindowStyle Minimized` : la fenêtre s'ouvre visible,
  limitation assumée.
- Module Go `github.com/thibsix/vibesync` ≠ remote `opmvpc/vibesync` → renommage
  mécanique à planifier quand aucun agent n'écrit.

## Fait (suite)

- Cœur C durci (11 findings sol, 973 checks) puis **UI GDI immediate-mode complète** :
  172 Ko, démarrage 47 ms, 0 % CPU idle, tout dessiné main (même l'encodeur PNG des
  captures). Essai réel 2 clients + vrai VLC concluant. VS-014 TERMINÉ.
- CI réécrite (QA Go + build/test C Windows + budget 500 Ko) : runs verts sur GitHub.
- Doc complète (agent Sonnet) : README, deploy-coolify.md, guides amis Win/mac.
  VS-007 terminé, VS-008 quasi (captures mac manquantes).
- Captures Windows câblées dans le guide amis.

## Fait (journée — sprint retours terrain → v0.2.0)

- **Prod** : serveur déployé sur Coolify (vibesync.choboai.com, mdp `onlyvibes`,
  auto-deploy sur push). Un deploy échoué (build tué, pas le code) diagnostiqué à
  la sonde `tools/probe` ; redéployé par Thibault, mdp vérifié actif.
- **Release v0.1** publiée (CI sur tag via gh, exe en asset). VS-016 terminé.
- Batch de retours terrain traité en tickets VS-017..026 : faux buffering (règles
  gelées dans la spec, garde-fous serveur déployés), UX connexion (machine à états
  conn, testeur de serveur, messages actionnables, arrêt sur mauvais mdp), settings
  in-app, sélection de texte, icône (SVG Opus → .ico maison), versions dans le
  welcome + bannière de mise à jour, room linger 30 min, file de chat hors ligne,
  reprise « salle vierge », mdp mémorisé via DPAPI.
- **Review terra high du delta Go** : 4 majeurs + 1 mineur → spec resserrée
  (af4c9f8 : file de chat liée à la salle, reprise vierge « précédemment
  connecté », anti-masquage buffering battement 1 s / figé diagnostiqué ≤ 5 s) ;
  corrections confiées à l'agent Go avec le flake CI
  `TestIntegrationDebitNormalNonAffecte` (repro locale 1/30).
- **Port C VS-017/024 livré et committé (1d5a4ad)** : suspension buffering, file
  de chat « en attente » dans l'UI, re-déclaration post-welcome, reprise vierge
  par emit_user_control, fix d'un bug miroir (ready effacé à la reconnexion).
  1 244 vérifications, 232 Ko. **Trouvaille de l'agent C** : le format des
  vecteurs n'encodait pas la convention event/eventKeep du générateur → champ
  `keepOutput` demandé au générateur Go, vecteur 13 rouge en attendant (seul
  échec toléré).
- VS-026 (dossiers médias à la Syncplay, demande de Thibault avec capture) :
  ticket créé, design gelé, assigné à l'agent C.

## À reprendre

Séquence de convergence v0.2.0 : rendu agent Go → retouche C vecteur 13 → VS-026
→ CI verte → sandbox → tag. Puis Mac (VS-015) et overlay OSD (VS-020).
Détails : `docs/STATUS.md`.
