# Journal — 2026-08-06

## Fait

- Review sol high du cœur C : 2 bloquants concurrence WinHTTP + 9 autres → fixes en cours.
- Client Swift complet livré (ui/macos, ~3 600 lignes, moteur + vecteurs + UI + build
  script + docs/build-macos.md) — à compiler sur le Mac.
- Test réel double-VLC dans **Windows Sandbox** (idée de Thibault, harnais
  `scripts/run-real-sandbox.ps1`) : 1er run ÉCHEC → découverte de la course autoplay
  VLC et de la lenteur du nudge au départ ; 2 règles gelées dans la spec ; fixes Go ;
  **2e run PASS 7/7 (drift 0,25 s)**. VS-006 terminé.
- Coupure de quota Claude vers 2h20 → les 3 agents relancés de leur transcript au
  retour. Thibault a créé le remote `github.com/opmvpc/vibesync` et committé l'état
  intermédiaire (bf72f5f) ; désormais **push après chaque commit**.

## Décisions

- Windows Sandbox = outil standard de validation réelle (jetable, VLC du host mappé
  lecture seule, réseau coupé, auto-extinction).

## Problèmes → solutions

- `.ps1` avec here-strings écrit en LF → erreur de parsing PowerShell 5.1 (« terminateur
  manquant ») → convertir en CRLF (noté en mémoire persistante).
- WindowsSandbox.exe ignore `-WindowStyle Minimized` : la fenêtre s'ouvre visible,
  limitation assumée.
- Module Go `github.com/thibsix/vibesync` ≠ remote `opmvpc/vibesync` → renommage
  mécanique à planifier quand aucun agent n'écrit.

## Fait (suite)

- Cœur C durci (11 findings sol, 973 checks) puis **UI GDI immediate-mode complète** :
  172 Ko, démarrage 47 ms, 0 % CPU idle, tout dessiné main (même l'encodeur PNG des
  captures). Essai réel 2 clients + vrai VLC concluant. VS-014 TERMINÉ.
- CI réécrite (QA Go + build/test C Windows + budget 500 Ko) : runs verts sur GitHub.
- Doc complète (agent Sonnet) : README, deploy-coolify.md, guides amis Win/mac.
  VS-007 terminé, VS-008 quasi (captures mac manquantes).
- Captures Windows câblées dans le guide amis.

## Fait (journée — sprint retours terrain → v0.2.0)

- **Prod** : serveur déployé sur Coolify (vibesync.choboai.com, mdp `onlyvibes`,
  auto-deploy sur push). Un deploy échoué (build tué, pas le code) diagnostiqué à
  la sonde `tools/probe` ; redéployé par Thibault, mdp vérifié actif.
- **Release v0.1** publiée (CI sur tag via gh, exe en asset). VS-016 terminé.
- Batch de retours terrain traité en tickets VS-017..026 : faux buffering (règles
  gelées dans la spec, garde-fous serveur déployés), UX connexion (machine à états
  conn, testeur de serveur, messages actionnables, arrêt sur mauvais mdp), settings
  in-app, sélection de texte, icône (SVG Opus → .ico maison), versions dans le
  welcome + bannière de mise à jour, room linger 30 min, file de chat hors ligne,
  reprise « salle vierge », mdp mémorisé via DPAPI.
- **Review terra high du delta Go** : 4 majeurs + 1 mineur → spec resserrée
  (af4c9f8 : file de chat liée à la salle, reprise vierge « précédemment
  connecté », anti-masquage buffering battement 1 s / figé diagnostiqué ≤ 5 s) ;
  corrections confiées à l'agent Go avec le flake CI
  `TestIntegrationDebitNormalNonAffecte` (repro locale 1/30).
- **Port C VS-017/024 livré et committé (1d5a4ad)** : suspension buffering, file
  de chat « en attente » dans l'UI, re-déclaration post-welcome, reprise vierge
  par emit_user_control, fix d'un bug miroir (ready effacé à la reconnexion).
  1 244 vérifications, 232 Ko. **Trouvaille de l'agent C** : le format des
  vecteurs n'encodait pas la convention event/eventKeep du générateur → champ
  `keepOutput` demandé au générateur Go, vecteur 13 rouge en attendant (seul
  échec toléré).
- VS-026 (dossiers médias à la Syncplay, demande de Thibault avec capture) :
  ticket créé, design gelé, assigné à l'agent C.

## Fait (fin de sprint — v0.2.0)

- **Rendu agent Go intégré (c811207)** : 5 findings terra fermés + flake CI
  élucidé (l'horloge simulée du test dépassait la consommation serveur → rafale
  écrasée sur un instant → token bucket saturé → « flood » légitime ; fix par
  barrière sync() par message, vérifié -count=50). Vecteur 13 régénéré :
  `keepOutput` (la convention event/eventKeep du générateur n'était pas encodée
  — trouvaille du port C), `scenario` (préconditions), écho exact 1800.8.
- **Rendu agent C intégré (3a70fa0)** : VS-026 complet (media.c, recherche
  bornée sur le thread VLC, bandeaux empilables, essai réel 2 clients
  concluant) + resserrages terra portés (reprise vierge sur position de salle
  observée décidée avant adoption du welcome, anti-masquage 1 s, file de chat
  liée à la salle). 1 282 vérifications test+asan, 13/13 vecteurs, 242 Ko.
- **CI entièrement verte** (première fois du sprint), **sandbox PASS 7/7 drift
  0,10 s**, **tag v0.2.0 poussé** → release GitHub via CI. Bureaucratie clôturée
  (VS-017, 021..026 terminés).

## Fait (soirée — reprise post-coupure, sur le Mac)

- Reprise via docs/REPRISE.md (supprimé une fois consommé). Renommage module Go
  thibsix→opmvpc committé (f676295) — CI verte après re-run (flake infra GitHub
  « Failed to resolve action download info », 3 occurrences aujourd'hui : NE PAS
  chercher un bug avant d'avoir relancé).
- **VS-015 TERMINÉ** (73499fc, agent Opus + review croisée codex terra) : moteur
  Swift réaligné sur les vecteurs régénérés → 13/13 ; ports VS-023/025/026/028 ;
  33 tests XCTest ; binaire 1,1 Mo. La review terra a sorti 6 findings (dont
  setFile déclaré avant l'attache VLC — VLCClient.prepare porté de
  internal/vlc/prepare.go pour observer la pause réelle).
- **Harnais de test réel macOS** `scripts/run-real-macos.sh` (équivalent local
  de la sandbox Windows, idée : 2 instances + HOME/suite isolés — le partage
  d'UserDefaults ferait se voler le jeton VS-028) + mode AutoPilot par env
  (VIBESYNC_AUTO_URL…, fichier de commandes, état JSON 1 Hz). **PASS 10/10
  contre la prod, 2 vrais VLC, écart final 0,004 s** (reproduit par
  l'orchestrateur). 3 vrais bugs trouvés : (1) BLOQUANT reconnexion distante —
  la boucle testait ws.isOpen, faux pendant le handshake → dial relancé toutes
  les 200 ms, connexion TLS jamais aboutie (invisible en local !) ; (2) VLC
  macOS refuse --no-one-instance (option Windows-only) et ne démarrait pas ;
  (3) App Nap ralentissait le poll en arrière-plan → beginActivity tenu.
- **VS-028 part C + blindage VS-029 livrés** (8b2982d, agent Opus codant À
  L'AVEUGLE depuis le Mac — pas de toolchain Windows locale — review terra puis
  CI verte du premier coup) : jeton persisté ini avec éviction si plein,
  net_close_graceful (un seul propriétaire des handles WinHTTP — le thread
  réseau ferme ses propres handles, close 1000 vérifiée par mini-serveur en
  test), 9 drapeaux VLC ajoutés dont --no-one-instance-when-started-from-file
  (VRAI par défaut : cause racine probable du retour terrain), vs_log.
- **Analyse couche C commune** (docs/research/2026-08-06-analyse-couche-c-commune.md,
  agent Sonnet) sur proposition de Thibault : engine/protocol/json/conn = 2 730
  lignes de C déjà portables, ~1 720 lignes Swift dupliquées à terme supprimées,
  5 phases sûres (vecteurs verts en permanence), risque n°1 = UTF-16 dispersé.
  Découverte bonus : conn.c (normalisation URL, politique retry) jamais porté en
  Swift — écart UX comblé gratuitement par la couche commune.
- Outillage Mac : UTM installé (Fusion impossible sans compte Broadcom, cask
  supprimé), ISO Win11 ARM64 25H2 FR officielle en téléchargement → VM de dev/
  test Windows (M1 Pro : pas de virtualisation imbriquée, snapshots en guise de
  sandbox).

## Décisions (soirée)

- Couche applicative C commune aux deux clients validée sur le principe par
  Thibault → ADR-010 à rédiger, implémentation par phases après stabilisation.
- Harnais macOS : 2 instances isolées sur une machine plutôt que VM jetable
  (pas d'équivalent Windows Sandbox sur macOS ; Tart possible plus tard).
- VS-020 (overlay OSD) explicitement différé par Thibault jusqu'au retour d'un
  environnement Windows.

## À reprendre

ADR-010 + tickets couche C commune (analyse prête) ; VM Win11 à monter (ISO en
cours) puis critères Windows-only de VS-029 et tag v0.2.1 ; reliquats : aligner
internal/vlc/launch.go sur les 9 drapeaux, toast reprise UI mac, captures mac
guide amis. Détails : `docs/STATUS.md`.
