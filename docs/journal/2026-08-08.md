# Journal — 2026-08-08

## Fait (sprint reliquats — tout ce qui ne demande pas de vrai PC Windows)

Décision de Thibault : .app en ad hoc définitif (pas de compte Apple Developer).
Cinq chantiers délégués en parallèle (agents Opus, clones VM jetables séparés),
review orchestrateur, commits atomiques par périmètre :

- **VS-035 TERMINÉ** (301d0d0) : UB `-(i64)acc` sur INT64_MIN corrigé (cas
  spécial 2^63, style i64_to_str), contrat de str_to_i64 documenté dans base.h,
  suppression retirée de ubsan.supp (contrôle négatif : l'ancien code déclenche
  bien UBSan sans elle). 959 checks asan+ubsan (+16), 14/14 vecteurs.
- **ini_flush durci** (f73eae9) : échecs d'écriture de vibesync.ini enfin
  visibles — code Win32 journalisé (3/5/32/112, écriture partielle), toast une
  fois par session, « Mot de passe oublié » mensonger corrigé. 1535 checks VM
  (+6). Suite : ticket **VS-037** (écriture atomique temporaire+MoveFileExW,
  priorité basse, 5f69c0a).
- **internal/vlc/launch.go aligné** (d09996b) : les 15 drapeaux VS-029 portés
  au driver Go, launchArgs(goos) extrait et gelé par test (15 Windows /
  12 darwin-linux). Exclusions MESURÉES sur VLC macOS 3.0.23 : la famille
  one-instance (3) est refusée, bloc conditionnel Windows/D-Bus de libvlc.
  go build/vet/test/staticcheck verts dans la VM.
- **Trouvaille en chemin → client Swift aligné aussi** (b4e614d) : le mac
  n'avait que 5 drapeaux sur 12 — dont PAS --lua-intf=http, LE filet contre un
  vlcrc où Syncplay a mis luaintf en interface principale (le scénario exact du
  retour terrain). VLCLauncher.launchArgs extrait, 4 tests de gel (45 verts),
  séance réelle prod PASS 10/10. --start-paused reconfirmé inopérant sur macOS
  (prepare reste l'autorité). Reste ouvert : un gel COMMUN aux 3 listes
  (vecteur launch-args.json) — les 3 gels sont indépendants.
- **Captures mac du guide amis** (c0449e3) : autorisation Enregistrement
  d'écran accordée par Thibault → écran connexion (instance isolée
  VIBESYNC_SUITE, préremplie) et écran salle pris sur une vraie séance
  AutoPilot 2 clients contre la prod (badge nudge visible). Pièges notés :
  cfprefsd ignore un HOME forgé (passer par VIBESYNC_SUITE), `defaults write
  <chemin>` ajoute lui-même .plist.
- **Icône de VibeSync.app** (5788f5a, question de Thibault — rien à voir avec
  la signature) : .icns 0,91 Mio rastérisé du assets/icon.svg par WebKit
  (qlmanage décore ses vignettes → infidèle ; genicon impossible sans Go
  local), fidélité vérifiée pixel à pixel contre icon-256.png, câblé avant le
  codesign, LaunchServices confirmé (NSWorkspace). Bundle 2,0 Mio.

## À reprendre

Nécessite un vrai PC Windows x86_64 : VS-020 (overlay OSD), revérification du
piège one-instance, asan du C Win32. Ouverts : VS-037 (basse), gel commun des
listes de drapeaux (idée). Prochaine release : v0.2.2 quand ça vaudra le coup
(l'icône et les drapeaux Swift ne sont pas encore dans une release).
