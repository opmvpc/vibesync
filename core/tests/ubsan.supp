# Suppressions UndefinedBehaviorSanitizer de la suite C (scripts/test-core-macos.sh).
#
# VIDE, ET ÇA DOIT LE RESTER. L'unique entrée historique
# (signed-integer-overflow:base_core.c) couvrait la négation de 2^63 dans
# str_to_i64 ; VS-035 a corrigé le calcul, la suppression a disparu avec lui.
#
# Toute remontée d'UBSan doit faire ÉCHOUER la suite : n'ajoutez pas de ligne
# ici sans ticket.
